import io

# Obtiene el tamaño predeterminado del buffer de entrada/salida en bytes
tamanio_buffer = io.DEFAULT_BUFFER_SIZE

print(f'Tamaño del buffer de entrada/salida predeterminado: {tamanio_buffer} bytes')
